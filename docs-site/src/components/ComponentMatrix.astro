---
import { CATALOG_COMPONENTS } from "../../../src/catalog/registry";
---

<section class="component-matrix">
  <h1>Components</h1>
  <p>Standard layout, input, data, feedback, and workspace primitives.</p>
  <ul>
    {CATALOG_COMPONENTS.map((entry) => <li><a href={`../components/${entry.id}/`}>{entry.title.en}</a></li>)}
  </ul>
</section>
