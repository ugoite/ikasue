---
---

import ComponentMatrix from "../../../../components/ComponentMatrix.astro";

The current runtime inventory is grouped by responsibility. Read the separate [Design philosophy](../philosophy/) page for the plane model, then use each component page for its contract, properties, and implementation sources.

<ComponentMatrix />

## How to use the matrix

Open a component page for practical usage guidance, properties, keyboard semantics, accessibility requirements, a copyable JSON contract, and its page-local interactive demo. The demo is fixed to that page’s component, so it does not create a second component page.
