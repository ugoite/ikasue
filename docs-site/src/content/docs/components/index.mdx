---
title: Component matrix / コンポーネント一覧
description: The 24 runtime components and their planar responsibilities.
---

import ComponentMatrix from "../../../components/ComponentMatrix.astro";

The runtime inventory is grouped by responsibility. The separate conceptual entry is [Design philosophy / 平面適応UIの思想](../philosophy/); this matrix is intentionally limited to the 24 runtime components.

<ComponentMatrix />

## How to use the matrix

Open a component page for the practical usage guidance, properties, keyboard semantics, accessibility contract, and a copyable JSON contract. Use the **Open catalog** link to change the matching catalog page directly with `?component=<id>`.
